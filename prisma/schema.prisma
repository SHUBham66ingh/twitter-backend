
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User{
  id    Int     @id @default(autoincrement())
  email String  @unique
  username String @unique
  firstname  String?
  lastName String?
}

model testing{
  id    Int     @id @default(autoincrement())
  num Int
  name String
}